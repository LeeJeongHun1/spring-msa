spring:
  application:
    name: eureka
  profiles:
    active: local
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
server:
  port: 8761
